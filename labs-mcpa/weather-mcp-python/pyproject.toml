[project]
name = "weather-mcp-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Behrad Eslamifar", email = "b.eslamifar@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2.0.0",
]

[project.scripts]
weather-mcp-python = "weather_mcp_python:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
